SD5976 process book

项目名称：Resonant Sips
课程：PolyU MSc IME - AI Tools for Creative Process and Transmedia (SD5976)
项目类型：AI 驱动叙事互动游戏（赛博朋克调酒叙事）
团队：Whale shark
文档版本：v2.0（叙述增强版，含流程图与素材插入窗口）

================================================================
封面后导读（建议保留在第一页）
================================================================

本 Process Book 记录的不是“我们最后做出了什么功能列表”，而是我们如何一步一步把一个想法变成可运行、可演示、可解释的系统。我们把课程中的 Storyworld 角色数据库接入游戏流程，设计了以情绪推断为核心的对话与调酒互动，并通过多轮迭代将项目从“概念拼贴”收敛为“稳定可玩的闭环”。这份文档重点回答课程中 "Document your process" 的核心问题：我们的设计决策从何而来、为什么要改、改了以后发生了什么、遇到的困难如何解决，以及团队在每一个关键节点如何协作推进。

================================================================
1. 项目起点：从创作问题到系统目标
================================================================

项目最初的创作问题是：如何让 AI 在叙事项目中不只是“自动回复”，而是成为真正参与互动机制的一部分。我们希望玩家不是看故事的人，而是影响故事的人。于是我们把玩家设定为调酒师，让玩家通过对话读取顾客状态，再通过调酒操作进行回应，最终让系统把“理解”转化为可感知的反馈（信任变化、小费变化、后续对话变化）。

我们选择赛博朋克酒吧并不是单纯为了风格化。这个题材有天然的情绪冲突：人物表层与真实状态往往不一致，非常适合做情绪推断与误读-修正机制。同时，调酒这件事本身具有可参数化特征，厚度、甜度、烈度可以作为情绪映射容器，让抽象情绪在游戏中变成可操作的目标。

从第一天开始，我们给项目设定了一个清晰目标：系统必须跑得起来，流程必须讲得清楚，所有关键能力必须可复现。这意味着我们不追求单点炫技，而是追求一个可验证的完整闭环。

================================================================
2. 从 0 到 1 的演进过程（叙述版时间线）
================================================================

在 3 月中旬，团队完成了最基础的工程骨架。我们建立了 React + Vite 前端、Node 存档服务、基础环境变量体系以及可运行脚本，让项目从“概念稿”进入“可执行原型”。同一阶段，我们把课程角色库 `polyu-storyworld` 接入为子模块，这为后续角色读取奠定了数据基础。

进入 4 月后，我们很快发现一个问题：项目功能看起来很多，但用户真正感受到的主线体验并不稳定。于是 4 月 16 日前后，团队做了关键战略调整：主动下线商店、章节推进、成就、图鉴等非核心模块，把开发资源集中到“顾客到来 -> 对话 -> 情绪判断 -> 调酒 -> 结算”这条主路径。这个决定在当时看似“做减法”，但实际上是让项目具备交付能力的转折点。

当主路径变清晰后，我们把重点放到课程最关键的部分：让顾客真正来自 Storyworld，而不是随机模板。团队新增了角色池配置流程，支持添加、勾选、移除角色 ID，并将顾客生成改为严格从 active 角色中选择。我们同时在服务端实现了 MCP 风格接口，打通了前端与角色数据库之间的结构化访问路径。

紧接着的迭代是情绪系统升级。我们将情绪统一为普拉奇克 8 维 schema，让情绪数据可以被稳定计算、存档和重用。更重要的是，这个情绪结果并非只用于展示，而是直接写入运行时状态，并参与调酒目标与结算逻辑。至此，项目从“AI 聊天 + 调酒界面”变成了真正有反馈闭环的互动系统。

在最后阶段，团队主要处理稳定性与演示可靠性问题：API 连通性提示、失败回退策略、对话复读抑制、TTS 文本严格同步、存档原子写入冲突修复等。这个阶段的目标非常明确：保证在课堂展示时，系统不仅“看起来会动”，而是“在不理想环境下也能稳定运行”。

================================================================
3. 我们如何让语言模型读取 Storyworld 数据库
================================================================

这是本项目最关键的技术过程。需要明确说明的是，我们没有训练新的模型参数，也没有做大规模微调。我们采取的是工具化读取与结构化推理路径：先读取角色数据库，再把角色信息变成受控输入，最后让模型在协议约束下输出可用结构。

具体来说，系统先在本地角色目录搜索目标角色；若本地不存在，再尝试子模块与远端回退源。读取到的 YAML/JSON 会统一映射成标准角色对象，包含名称、背景、性格、说话风格等字段。随后，情绪服务将这些字段拼装成分析文本并发送给模型，要求模型只输出 JSON。系统不会直接信任模型输出，而是执行后处理：缺失情绪补零、权重截断到合法范围、归一化、top3 重算。若模型失败，系统会自动回退到启发式算法，确保游戏流程不中断。

这种架构的核心价值是“可控与可追踪”。我们可以知道每一次情绪结果来自模型还是回退策略，也可以在出现异常时快速定位问题源头。对课程项目而言，这比单纯演示“模型能回答”更有过程价值，因为它体现了工程层面的可解释性。

----------------------------------------------------------------
流程图 A（系统架构总览）- 建议放在本节后
----------------------------------------------------------------

```mermaid
flowchart LR
    U[玩家输入] --> FE[前端 React + Pixi]
    FE --> API[save-server Node API]
    API --> SW[storyworld-service]
    API --> EMO[emotion-service]
    SW --> L1[本地角色 seeds]
    SW --> L2[polyu-storyworld 子模块]
    SW --> L3[远端回退 GitHub/HF]
    EMO --> M1[LLM 分析]
    EMO --> M2[失败回退 heuristic]
    API --> SAVE[本地 JSON 存档]
    SAVE --> FE
```

图注建议：图 A. Resonant Sips 系统架构总览（数据层、服务层、玩法层联动）

----------------------------------------------------------------
流程图 B（LLM 读取 Storyworld 的处理链）- 建议紧接图 A
----------------------------------------------------------------

```mermaid
flowchart TD
    Q[输入角色ID/名称] --> S1[本地检索角色]
    S1 -->|命中| N1[标准化角色对象]
    S1 -->|未命中| S2[远端回退读取 YAML]
    S2 -->|成功| C1[可选落盘缓存]
    C1 --> N1
    S2 -->|失败| E1[返回角色未找到]
    N1 --> P1[构建情绪分析提示]
    P1 --> A1[模型输出 JSON]
    A1 --> V1[后处理:补齐/归一化/top3重算]
    V1 --> R1[写入 currentEmotionWeights/top3]
    A1 -->|异常| F1[heuristic 回退]
    F1 --> V1
```

图注建议：图 B. 角色数据库读取与情绪分析的可靠性流程

================================================================
4. 设计取舍：为什么我们要不断做“减法”和“重构”
================================================================

项目开发过程中，最重要的并不是“加了多少功能”，而是“删掉了什么，为什么删”。我们在中期主动收缩了很多系统模块，因为它们虽然丰富了表面功能，却显著增加了状态复杂度和调试成本。对于课程展示阶段，最危险的不是功能不够多，而是核心链路不稳定。

我们最终把资源集中在三个原则上：第一，玩家每一步操作都要有明确反馈；第二，情绪机制必须进入真实结算逻辑；第三，任何外部依赖失败时流程都要可恢复。所有后续优化基本都围绕这三个原则展开，这也是为什么你会在提交历史中看到大量“重构、收口、修复边界”的改动。

================================================================
5. 关键挑战与解决路径（过程证据）
================================================================

第一个挑战是模型输出不稳定。模型偶尔会返回解释性文本、半截 JSON 或字段缺失，直接使用会破坏玩法判定。我们的解决方式是协议化输出 + 二次解析 + 强制清洗 + 回退机制。这个设计让情绪链路从“看运气”变成“可容错”。

第二个挑战是网络环境差异。部分环境下 GitHub、Hugging Face、模型端点访问不稳定，导致角色加载或情绪分析失败。我们通过本地优先、远端回退可开关、成功后缓存、失败可提示四层策略，显著降低了线上演示风险。

第三个挑战是状态污染与存档并发。早期出现过新开局继承旧状态、并发写入重命名失败等问题。我们修复了 legacy 回灌策略，并把写入改为原子写 + 随机后缀临时文件，确保数据一致性。

第四个挑战是对话体验问题。AI 在连续回合可能复读，流式首包前也会出现“像卡死”的空窗。我们通过 prompt 约束、采样惩罚和思考占位态优化了这一体验，让玩家感受更连贯。

================================================================
6. 游戏流程如何被设计成“可感知的情绪闭环”
================================================================

在玩法设计上，我们坚持一个核心思路：情绪不是彩色标签，而是能影响玩家策略的系统变量。玩家先在对话中观察角色，再做三情绪判断，再进入调酒操作，最后通过递酒结算看到反馈。猜中越多，收益越高；即使猜错也不会粗暴惩罚，而是以小幅正反馈引导学习。我们希望玩家感受到的是“理解角色会更顺”，而不是“答题失败就被打断”。

这个设计把叙事与机制连接起来：对话不再只是阅读文本，调酒也不再只是 UI 操作，而是成为情绪理解的行动化表达。

----------------------------------------------------------------
流程图 C（玩家视角的游戏主循环）- 建议放在本节末尾
----------------------------------------------------------------

```mermaid
flowchart TD
    G0[开始新游戏] --> G1[配置角色池]
    G1 --> G2[顾客到访]
    G2 --> G3[对话观察与提问]
    G3 --> G4[玩家选择3种情绪]
    G4 --> G5[进入调酒操作]
    G5 --> G6[递酒与结算]
    G6 --> G7[信任/小费/状态更新]
    G7 --> G8{继续当天?}
    G8 -->|是| G2
    G8 -->|否| G9[日结并进入下一天]
```

图注建议：图 C. 玩家体验主循环（叙事判断与调酒反馈闭环）

================================================================
7. 团队协作与贡献说明（叙述版）
================================================================

从仓库记录看，团队贡献呈现出明显的多线并行。核心玩法与服务收口由主开发线持续推进，UI 与素材整合由视觉体验线高频迭代，文档与合规线负责把技术事实转化为可提交证据。虽然每位成员的 commit 数量不同，但项目能在短周期内达到可演示状态，依赖的是三种角色之间的有效衔接：有人负责把系统“做出来”，有人负责把体验“做顺滑”，也有人负责把过程“讲清楚并可追溯”。

我们在协作策略上强调“先统一主链路，再并行细化”。这避免了多人同时改动核心状态逻辑造成冲突，也让每个阶段的验证范围更清晰。

================================================================
8. 验证与可靠性：我们如何证明它可运行
================================================================

我们采用的是“关键路径验证”而不是只看单点功能。每次重要改动后，都会检查以下路径：服务能否启动、角色能否读取、对话是否可持续、情绪是否可解析、调酒是否可结算、存档是否可回读。如果某一环失败，优先保证用户有明确错误提示和恢复入口，而不是让系统静默卡死。

对课程展示而言，可靠性本身就是设计成果。因为一个 AI 项目真正的难点并不只是生成内容，而是把不可控的生成能力纳入可控的交互系统中。

================================================================
9. 素材插入规划（你提供截图与 Demo 后直接按位放置）
================================================================

以下是你可以直接在 Word 中操作的“窗口化插入方案”。每个窗口都给了明确位置、建议内容和图注模板。你只需要把现有素材按编号放入即可。

----------------------------------------------------------------
截图窗口 S1：主界面/世界观建立
----------------------------------------------------------------
放置位置：第 1 章结尾（“项目起点”最后一段之后）
建议内容：游戏主菜单或进入酒吧前的视觉画面
图注模板：图 S1. Resonant Sips 主界面与世界观入口
作用说明：用于展示项目的叙事氛围与视觉基调

----------------------------------------------------------------
截图窗口 S2：新游戏配置页（角色池管理）
----------------------------------------------------------------
放置位置：第 2 章“角色池接入”相关段落后
建议内容：添加角色 ID、勾选 active 角色、开始新游戏按钮
图注模板：图 S2. 新游戏角色池配置流程
作用说明：证明“顾客来自课程角色库”这一关键过程

----------------------------------------------------------------
截图窗口 S3：Storyworld 角色读取结果
----------------------------------------------------------------
放置位置：第 3 章“读取数据库”段落后，流程图 A 之前
建议内容：角色成功加载后的界面（含姓名、头像或设定信息）
图注模板：图 S3. Storyworld 角色读取成功示例
作用说明：展示数据库读取到游戏状态的落地结果

----------------------------------------------------------------
截图窗口 S4：对话与情绪判断界面
----------------------------------------------------------------
放置位置：第 6 章“情绪闭环”第一段后
建议内容：聊天面板 + 玩家三情绪选择 UI
图注模板：图 S4. 对话理解与情绪选择环节
作用说明：展示“观察 -> 判断”的关键互动步骤

----------------------------------------------------------------
截图窗口 S5：调酒操作界面
----------------------------------------------------------------
放置位置：第 6 章“进入调酒操作”描述后
建议内容：Pixi 调酒面板、材料选择或三维参数界面
图注模板：图 S5. 调酒执行阶段界面
作用说明：展示情绪判断如何转化为可操作行为

----------------------------------------------------------------
截图窗口 S6：递酒结算与反馈
----------------------------------------------------------------
放置位置：第 6 章末尾（流程图 C 前或后均可）
建议内容：信任变化、小费结果、离店反馈
图注模板：图 S6. 递酒结算与情绪反馈结果
作用说明：展示闭环中的“结果反馈”是否可感知

----------------------------------------------------------------
截图窗口 S7：异常恢复或错误提示
----------------------------------------------------------------
放置位置：第 5 章“挑战与解决”对应段落后
建议内容：网络失败提示、返回配置页按钮、恢复入口
图注模板：图 S7. 异常场景下的可恢复设计
作用说明：强调工程可靠性与用户体验保障

----------------------------------------------------------------
Demo 窗口 D1：完整游玩流程视频
----------------------------------------------------------------
放置位置：正文末尾“附录前”或“附录后独立页”
建议形式：
1) 插入一张视频封面截图；
2) 在封面下方放 Demo 链接（或二维码）；
3) 用一句话说明视频时长与演示范围。

文案模板：
Demo D1（完整流程）：从角色配置到日结的完整游玩演示（建议 3-5 分钟）
链接：<你的视频链接>

----------------------------------------------------------------
Demo 窗口 D2：技术链路演示视频（可选）
----------------------------------------------------------------
放置位置：第 3 章之后
建议内容：角色 ID 输入 -> 角色读取 -> 情绪分析结果写入的过程录屏
文案模板：
Demo D2（技术链路）：Storyworld 读取与情绪分析链路验证（建议 1-2 分钟）
链接：<你的视频链接>

================================================================
10. 伦理、版权与学术规范
================================================================

我们在项目中使用的角色来源于课程相关角色库，原始角色版权归原作者所有。团队在文档中保留了来源披露、使用边界和非商业教学用途说明。我们同时采用本地密钥注入策略，避免真实 API 密钥进入版本库。对我们而言，过程记录不仅包括技术实现，也包括创作伦理和数据责任，这是课程项目中不可分割的一部分。

================================================================
11. 结论与反思
================================================================

回看整个过程，Resonant Sips 的核心收获并不是“把很多 AI 功能塞进游戏”，而是学会了如何把不稳定的生成能力组织进一个稳定的交互系统。我们从一开始的功能发散，走向中期的结构收敛，再走向后期的可靠性强化。每一步优化都围绕同一个问题：玩家是否真的能感知到自己的判断与行动在系统中产生了变化。

因此，这份 Process Book 想表达的最终结论是：一个课程项目真正的成熟，不是“做得多”，而是“做得通、讲得清、可复现、可负责”。我们把 Storyworld 数据、语言模型推理、情绪机制和调酒互动连接成闭环，也把每一次取舍、修正和验证转化成了可追溯的过程证据。这正是我们对 "Document your process" 的完整回答。

================================================================
12. Document your process（Reflection in action - 逐题回应）
================================================================

Q1. How did AI tools affect your production pipeline?

AI 工具并没有只是“加速写文本”，而是重塑了整个生产流水线。传统流程通常是“先写死剧情，再做交互”，而我们的流程变成了“先建立结构化角色输入，再通过模型生成候选输出，再由规则层清洗并映射到游戏机制”。这意味着内容生产从一次性创作转为持续迭代：每一次角色读取、情绪分析、对话响应都成为一个可观察、可修正的过程节点。团队开发节奏也因此发生变化，工程实现、玩法设计和文档维护不再是线性关系，而是围绕同一条数据链路并行推进。我们必须在每一次迭代里同时考虑 Prompt 约束、输出结构、容错策略和玩家体验，这直接推动了项目从“功能实现”升级为“系统编排”。

Q2. What were the implications of including AI tools in your output?

把 AI 纳入最终输出带来了两类影响。第一类是创作层面的扩展：顾客对话与情绪状态不再是固定脚本，玩家每次互动都可能触发不同走向，项目因此获得了更强的动态性和重玩价值。第二类是责任层面的要求：AI 输出具有不确定性，如果没有协议约束和回退机制，系统稳定性会快速下降。我们因此引入了结构化 JSON 输出、8 维情绪校验、模型失败回退、错误可视化、数据来源披露等机制，确保 AI 能力被纳入可控边界。换句话说，AI 让结果更有表现力，也迫使我们对可靠性、可解释性和伦理合规承担更高标准。这种“能力提升与责任上升并存”的现实，正是本项目最核心的反思结论。

================================================================
附录：证据索引（代码与文档路径）
================================================================

运行与总览文档：
- README.md
- README.zh-CN.md

流程与设计文档：
- DOC/DESIGN_PLAN.md
- DOC/流程-v1.md
- DOC/已完成项汇总-2026-04-16.md

核心服务与数据接入：
- server/storyworld-service.mjs
- server/emotion-service.mjs
- server/save-server.mjs
- src/utils/storyworldRepository.js
- src/utils/ai/customerGeneration.js

伦理与资产说明：
- ETHICS_AND_USE.md
- ASSET_ATTRIBUTION.md
- seeds/characters/README.md
SD5976 process book

项目名称：Resonant Sips
课程：PolyU MSc IME - AI Tools for Creative Process and Transmedia (SD5976)
项目类型：AI 驱动叙事互动游戏（赛博朋克调酒叙事）
团队：Whale shark
文档版本：v1.0（基于仓库当前实现与提交记录整理）

================================================================
一、文档目的与对应课程要求（Document your process）
================================================================

本 Process Book 的目标不是只展示“最后做成了什么”，而是完整呈现：

1) 我们如何从概念出发，逐步搭建可运行的系统；
2) 我们如何把课程提供的 Storyworld 角色数据真正接入玩法；
3) 我们如何让语言模型在项目中“读懂角色数据库”并服务对话与情绪机制；
4) 我们每一次优化背后的原因、目标和取舍；
5) 我们遇到的关键挑战、失败路径与最终解决方案；
6) 我们如何做验证、如何处理伦理与版权边界。

换句话说，这份文档强调“过程证据（process evidence）”而非“结果陈述（result-only）”。

================================================================
二、项目起点：问题定义与创作动机
================================================================

2.1 初始问题

我们一开始并不想做“只有聊天框的 AI Demo”，而是希望做一个有明确互动反馈的体验：

- 玩家不是旁观者，而是“调酒师”；
- 玩家通过对话观察顾客状态，再用调酒行为回应情绪；
- 系统要把“角色设定、情绪推断、互动操作、结果反馈”串成闭环。

2.2 为什么选择“调酒 + 叙事 + AI”

我们选择该方向有三个创作目的：

- 可解释性：调酒三维（厚度/甜度/烈度）天然适合作为情绪映射载体；
- 叙事张力：顾客在赛博朋克城市中带着创伤、压力、希望，适合做情绪读写；
- 课程契合：可自然接入 Storyworld 角色库，体现“外部角色资产 -> 交互系统”的整合能力。

2.3 项目总目标（从构想到落地）

我们把目标定义为：
"做出一个可运行、可演示、可复现、可解释的 AI 叙事玩法系统，而非单点功能拼贴。"

================================================================
三、团队协作与贡献结构（基于仓库历史记录）
================================================================

3.1 提交概况（仓库证据）

- 仓库总提交数：134 commits
- 主要提交作者（按 commit 计数）：
  - BAO-Hongzhen：74
  - tangjian89757：43
  - ZHANG Mingzhi：9
  - emo / emovon-123：7（含历史账号名）

说明：commit 数反映的是代码提交频次，不完全等于全部工作量（如设计讨论、素材挑选、测试与课堂演练等也有大量隐性工作）。

3.2 分工画像（从代码变化区域反推）

从高频变更文件看，团队工作主要分为四条线：

A. 核心玩法与流程线
- 代表文件：src/pages/GamePage.jsx, useCustomerDayHandlers, useServeProgressHandlers
- 工作内容：日流程、顾客轮次、结算、界面状态流转

B. AI 对话与情绪线
- 代表文件：src/components/Chat/ChatPanel.jsx, src/config/api.js, src/utils/ai/customerGeneration.js, server/emotion-service.mjs
- 工作内容：模型调用、输出清洗、情绪分析协议、对话体验优化

C. Storyworld 数据接入线
- 代表文件：server/storyworld-service.mjs, src/utils/storyworldRepository.js, server/save-server.mjs
- 工作内容：YAML 读取、远端回退、MCP 风格接口、角色缓存

D. 视觉与体验线
- 代表文件：HomePage / ChatPanel / overlay 样式与素材资产
- 工作内容：UI 风格统一、动画、音效、素材整合与演示表现

================================================================
四、从 0 到 1 的演进时间线（核心里程碑）
================================================================

4.1 阶段 P0：基础架构搭建（3 月中旬）

目标：让项目能运行、能迭代。

已完成：
- 建立 React + Vite 前端与 Node 存档服务；
- 建立基础运行脚本（dev/build）；
- 建立中英 README、环境变量模板与本地配置说明；
- 接入 polyu-storyworld 子模块（作为课程角色库入口）。

为什么先做这个：
- 没有可运行骨架，后续 AI 与玩法优化无法验证；
- 团队协作必须先统一“能跑”的基础环境和目录结构。

4.2 阶段 P1：玩法收敛（4 月 16 日前后）

目标：把系统从“想做很多功能”收敛到“可玩的核心闭环”。

已完成：
- 下线商店、章节推进、图鉴、成就、奖励事件等干扰项；
- 固定核心循环：接待顾客 -> 情绪判断 -> 调酒 -> 结算 -> 次日；
- 日流程稳定化（每天固定顾客数、结束判定修正）。

为什么这样优化：
- 早期功能面太散，用户体验和课堂演示都不聚焦；
- 先保核心可玩性，后做扩展，避免“功能很多但都不稳”。

4.3 阶段 P2：角色池与 Storyworld 接入（4 月 16-17 日）

目标：让顾客真正来自课程角色库，而不是随机模板角色。

已完成：
- 新游戏配置页支持添加/勾选/移除角色 ID；
- 顾客生成切换为“仅来自 activeCharacterIds”；
- 新增 MCP 风格接口：
  - /api/mcp/character/get_by_name
  - /api/mcp/character/search
  - /api/mcp/emotion/analyze_character
- 服务端实现本地优先 + 远端回退 + 可选落盘缓存。

为什么这样优化：
- 课程强调角色资产与过程证据，必须确保角色来源可追溯；
- 角色池可控后，测试与演示可复现，避免随机性导致结果不稳定。

4.4 阶段 P3：8 维情绪系统与调酒联动（4 月 17 日后）

目标：把“情绪分析”真正接入游戏规则，而不是摆设。

已完成：
- 采用普拉奇克 8 情绪（joy/trust/fear/surprise/sadness/disgust/anger/anticipation）；
- 输出后处理规则：缺失补 0、clamp、归一化、top3 系统重算；
- 将 top3 写入运行时角色状态（currentEmotionWeights/currentEmotionTop3）；
- 玩家 3 情绪选择驱动调酒目标与奖励分档。

为什么这样优化：
- 统一情绪 schema 才能保证对话、调酒、结算共享同一语义；
- 强后处理能降低模型输出不稳定对玩法链路的破坏。

4.5 阶段 P4：体验与工程稳定化（4 月下旬）

目标：提升演示可靠性和团队交付质量。

已完成：
- AI 连通性错误可视化；
- 远程 TTS 与严格文本同步保护；
- 对话等待态（思考省略号）和防复读策略；
- 存档原子写入冲突修复；
- 网络条件（CN/HK）切换文档与脚本；
- 伦理与版权文档落地。

为什么这样优化：
- 课堂演示环境不可控（网络、密钥、终端性能），必须优先“稳态可运行”；
- 可观测性与降级策略是 AI 项目中最关键的工程保障。

================================================================
五、关键技术问题：我们如何让“语言模型读取 Storyworld 数据库”
================================================================

5.1 我们没有做参数微调，而是做“工具化读取 + 结构化上下文注入”

这是本项目最容易被误解的地方，我们在此明确：

- 我们没有训练一个新的大模型权重；
- 我们做的是“让运行中的模型通过后端工具读取角色数据库，再做推理”；
- 该方法本质是：检索增强 + 工具调用 + 协议约束（而非 fine-tuning）。

5.2 数据层（Storyworld）

角色来源优先级：
1) 本地角色档（seeds/characters/presets, added）
2) 子模块 polyu-storyworld
3) 远端回退（GitHub raw / Hugging Face 索引）

关键设计点：
- 角色 ID 做安全校验（防路径穿越）；
- YAML/JSON 多格式统一映射为标准角色对象；
- 命中远端后可缓存到本地，减少重复请求与网络抖动影响。

5.3 服务层（MCP 风格 API）

通过 save-server 暴露统一接口：
- character.get_by_name（HTTP 路由：/api/mcp/character/get_by_name）
- character.search（HTTP 路由：/api/mcp/character/search）
- emotion.analyze_character（HTTP 路由：/api/mcp/emotion/analyze_character）

这样做的目的：
- 让前端不用直接处理 YAML 与远端源细节；
- 让“角色读取”与“情绪分析”成为可复用能力；
- 保持服务边界清晰，便于后续替换数据源或模型。

5.4 推理层（emotion-service）

流程是：
1) 从角色对象提取可分析文本（姓名、背景、人格、说话风格等）；
2) 给模型下达严格 JSON 输出协议；
3) 对返回结果做强清洗（8 维补齐、归一化、top3 重算）；
4) 若模型失败，则回退关键词启发式分析，保证流程不中断。

核心收益：
- 即使模型偶发格式错误，游戏仍然可运行；
- 情绪结果可追踪 source（model/heuristic）与 sourceHash，便于调试与复现。

5.5 玩法层（customerGeneration）

情绪结果并非只用于展示，而是写入运行时状态：
- currentEmotionWeights
- currentEmotionTop3

并进一步影响：
- emotionMask（表象/真实情绪）
- 玩家猜测反馈
- 调酒目标生成
- 信任与小费结算

这使“读数据库 -> 情绪推理 -> 互动反馈”形成闭环，不是单向调用。

================================================================
六、为什么要这样优化：关键决策与理由
================================================================

6.1 从“功能堆叠”转向“闭环优先”

问题：
- 初期系统存在商店、章节、图鉴等分支，核心玩法注意力被稀释。

决策：
- 暂时下线非核心系统，专注顾客-对话-情绪-调酒-结算链路。

目的：
- 提升可玩性清晰度；
- 降低 debug 面积，提高交付稳定性。

6.2 从“随机顾客兜底”转向“严格角色池”

问题：
- 随机兜底会让测试结果不一致，也削弱课程角色资产价值。

决策：
- activeCharacterIds 为空直接报错并引导返回配置页，不再默认补位。

目的：
- 让每次演示都可控、可复现；
- 强化“角色来源可解释”。

6.3 从“自由文本情绪”转向“8 维结构化协议”

问题：
- 自由文本难以直接驱动数值玩法，且难做一致性验证。

决策：
- 统一到 8 维固定 ID，强制 JSON 协议，系统重算 top3。

目的：
- 让情绪结果可计算、可落盘、可对齐玩法规则；
- 便于后续 A/B 调参和日志分析。

6.4 从“单一路径成功”转向“失败可恢复”

问题：
- AI 系统天生不稳定，网络/密钥/模型都可能失败。

决策：
- 实施多层降级：模型失败 -> heuristic 回退；远端失败 -> 本地缓存；
- UI 增加恢复出口（返回配置页、返回上一级）；
- 错误信息可视化而非静默失败。

目的：
- 保证课堂演示时“不会卡死”；
- 降低现场故障风险，提升可信度。

================================================================
七、关键挑战与解决过程（含失败经验）
================================================================

7.1 挑战一：模型输出格式不稳定

现象：
- 模型可能返回解释文本、半截 JSON、字段缺失。

影响：
- 直接破坏情绪链路，导致玩法判定异常。

解决：
- 双层解析（完整 JSON + 片段提取）；
- 不完整时重试/回退；
- 缺失字段自动补齐并归一化。

经验：
- AI 输出不能“信任即用”，必须把协议与清洗做成硬规则。

7.2 挑战二：角色读取受网络条件影响

现象：
- 部分网络环境下访问 GitHub/Hugging Face 失败。

影响：
- 角色加载不稳定，首位顾客可能生成失败。

解决：
- 本地优先读取；
- 远端回退开关可控；
- 远端成功后本地缓存；
- 提供 CN/HK 网络配置文档与一键切换脚本。

经验：
- 数据接入不是“接上就行”，必须针对部署环境设计可切换策略。

7.3 挑战三：会话与存档状态污染

现象：
- 新游戏可能被旧槽位 legacy 状态污染，直接跳过流程。

解决：
- 回灌前清理并保留关键角色池键；
- 存档写入改为原子写 + 随机后缀临时文件，避免并发冲突。

经验：
- 叙事游戏最怕状态错乱，存档设计要比普通 Demo 更谨慎。

7.4 挑战四：对话体验出现复读与等待空白

现象：
- AI 回复重复；流式首包前用户误以为卡死。

解决：
- Prompt 增加“最近两条回复禁复读”约束；
- 采样惩罚参数优化；
- 思考中占位文案（省略号）与首包自动替换。

经验：
- “可感知体验”比模型参数本身更影响用户满意度。

================================================================
八、验证方法与证据链
================================================================

8.1 功能验证（手动清单）

我们采用“关键路径逐项验证”：

- 应用可启动（前端 5173 + 服务 3001）；
- /health 与 /api/mcp 路由可响应；
- 新游戏可添加角色并进入首位顾客；
- 对话、情绪显示、调酒流程可走通；
- 递酒后结算可触发，存档可写可读。

8.2 结构一致性验证

- 情绪 ID 严格限制在 8 种集合；
- 权重归一化；
- top3 由系统重算，不依赖模型原字段；
- 角色 ID 输入做格式校验与安全边界检查。

8.3 工程验证

- 可构建（npm run build）；
- 路径安全检查脚本（check-runtime-asset-paths）；
- 文档、环境模板、网络配置指南同步维护。

================================================================
九、伦理、版权与学术规范
================================================================

9.1 角色版权与引用原则

- Storyworld 角色原始版权归原作者；
- 本项目使用场景为课程学习、研究与展示；
- 通过 ID 级引用与来源说明进行披露。

9.2 凭证与安全

- 真实 API key 不入库；
- 仅提交 .env.example；
- 团队成员本地使用 .env.local。

9.3 为什么这部分要写进 process

课程中的“process”不只包含技术步骤，也包含责任边界：
- 你如何使用他人角色资产；
- 你如何处理模型风险和数据来源合法性；
- 你如何确保可追溯与可解释。

================================================================
十、我们学到的核心方法论（反思）
================================================================

10.1 先做可运行闭环，再追求复杂功能

没有闭环，任何“高级功能”都只是碎片。先确保最短路径可玩，才有优化价值。

10.2 AI 系统要“默认会失败”

对模型输出、网络、存储都必须设计回退与恢复，不然演示场景会高度脆弱。

10.3 结构化比“提示词玄学”更可靠

固定 schema、强后处理、可追踪日志，远比单纯调 prompt 更稳定。

10.4 过程文档本身就是工程资产

Process Book 不是总结作文，而是“决策记录 + 证据索引 + 复现入口”。
它能直接帮助后续迭代、答辩和团队交接。

================================================================
十一、下一步迭代计划（可作为答辩后续工作）
================================================================

1) 自动化测试与 CI
- 为情绪协议、角色解析、关键路由增加基础测试；
- 建立最小 CI 保证核心链路回归。

2) dialogue.build_context 独立工具化
- 继续把对话上下文构建从业务逻辑中抽离，降低口吻漂移。

3) 情绪-调酒映射可配置化
- 从等权组合升级到可调权重，支持玩法难度与角色差异调参。

4) 观测体系增强
- 增加关键事件日志与命中率指标（模型/回退占比、平均响应时长等）。

================================================================
十二、附录：仓库证据索引（用于课堂说明）
================================================================

A. 项目总览与运行文档
- README.md
- README.zh-CN.md

B. 设计与流程文档
- DOC/DESIGN_PLAN.md
- DOC/流程-v1.md
- DOC/已完成项汇总-2026-04-16.md

C. Storyworld 与情绪服务核心实现
- server/storyworld-service.mjs
- server/emotion-service.mjs
- server/save-server.mjs
- src/utils/storyworldRepository.js
- src/utils/ai/customerGeneration.js

D. 伦理与合规文档
- ETHICS_AND_USE.md
- ASSET_ATTRIBUTION.md
- seeds/characters/README.md

================================================================
结语
================================================================

Resonant Sips 的价值不只在于“做出了一个 AI 游戏”，更在于我们把课程中的角色资产、情绪模型、交互设计与工程落地连接成一个可运行、可解释、可复现的创作流程。

这份 SD5976 process book 记录了我们如何从概念走到系统、从功能堆叠走向闭环、从不稳定走向可控，也记录了我们如何在创作中持续做取舍、修正和优化。

这就是我们对 Document your process 的实践回答。
